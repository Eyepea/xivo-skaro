# XXX at first, this won't be necessary since we can put this information
#     inside the generall provisioning configuration file

[general]
;; username -- the username to use to download files on cisco.com website
; username: foo
;; password -- the password to use to download files on cisco.com website
; password: bar
