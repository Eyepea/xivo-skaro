<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical">
    
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="wrap_content"
    android:layout_width="fill_parent"
    android:orientation="horizontal"
    android:paddingBottom="10dp">
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="wrap_content"
    android:id="@+id/history_fullname_title"
    android:padding="1dp"
    android:textSize="16sp"
    android:text="@string/history_fullname_title" >
</TextView>
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="90dp"
    android:layout_height="wrap_content"
    android:id="@+id/history_date_title"
    android:padding="1dp"
    android:textSize="14sp"
    android:maxLines="2"
    android:ellipsize="end"
    android:text="@string/history_date_title">
</TextView>
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="60dp"
    android:layout_height="wrap_content"
    android:id="@+id/history_duration_title"
    android:padding="1dp"
    android:textSize="14sp"
    android:maxLines="1"
    android:ellipsize="end"
    android:singleLine="true"
    android:text="@string/history_duration_title">
</TextView>
</LinearLayout>
    <!-- List Divider -->
    <View android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:background="?android:attr/listDivider" />

    <!-- ListView (grid_items) -->
    <LinearLayout android:id="@+id/layout"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">
        <ListView android:id="@+id/history_list"
            android:layout_height="fill_parent"
            android:layout_width="fill_parent">
        </ListView>
    </LinearLayout>

</LinearLayout>