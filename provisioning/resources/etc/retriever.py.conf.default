# Default configuration for the device retriever

mac_retriever = MacDeviceRetriever()
sn_retriever = SerialNumberDeviceRetriever()
ip_retriever = IpDeviceRetriever()
add_retriever = AddDeviceRetriever()
retriever = FirstCompositeDeviceRetriever([mac_retriever, sn_retriever, ip_retriever, add_retriever])
